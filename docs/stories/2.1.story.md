# Story 2.1 — F1: Mensageria Desacoplada com Service Bus + Functions .NET

> **Epic:** [EPIC-002 — Living Lab Workshop Azure-Native](../epics/EPIC-002-living-lab-workshop.md)
> **Status:** Ready
> **Created:** 2026-05-25 by @sm (River)
> **Validated:** 2026-05-25 by @po (Pax) — GO 9/10
> **Architecture reviewed:** 2026-05-25 by @architect (Aria) — GO ARCHITECTURE com 1 patch obrigatório aplicado
> **Foundational pattern:** [ADE-000](../architecture/ade-000-microservice-parallel-pattern.md) (7 invariantes herdadas por F2-F6)
> **Sprint context:** Primeira fase do workshop de 40h — porta de entrada da jornada cloud-native. Detalhada nos 9 sub-itens no blueprint.
> **Source:** [docs/workshops/2026-blueprint-living-lab-azure.md](../workshops/2026-blueprint-living-lab-azure.md) (seção 4.F1)

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "code-review"
  - "pattern-validation"
  - "manual-review"
deployment_owner: "@devops"
content_collaboration: "@analyst"
```

**Rationale:** Story híbrida — `@dev` produz código .NET das Functions + workflow CI/CD; `@analyst` colabora na produção dos 4 artefatos didáticos (README, PORTAL-GUIDE, SPEAKER-NOTES, slides); `@architect` valida padrões antes do merge.

---

## Story

**Como** aluno do workshop "Living Lab Azure-Native" (devs polyglot com background cloud, sem expertise .NET prévia exigida),
**eu quero** completar a Fase 1 do workshop em 6h provisionando Service Bus via Portal Azure e implementando 2 Azure Functions (.NET 8 isolated) que materializam um fluxo de compra v2 desacoplado por mensageria,
**para que** eu compreenda na prática a diferença entre comunicação síncrona vs assíncrona, anatomia do Service Bus, garantias de entrega at-least-once, idempotência no consumer, dead-lettering, e tenha a Function App + queue funcionando como base para todas as fases seguintes.

---

## Acceptance Criteria

1. **AC-1 — Branch criada:** `phase-01-servicebus-functions` partindo de `main` (estado pré-workshop), com workflow CI/CD próprio `.github/workflows/deploy-phase-01.yml`.

2. **AC-2 — Recursos Azure provisionados via Portal:** aluno demonstra navegação Portal → Resource Group `rg-fifa2026-workshop-<iniciais>` → Service Bus namespace `sb-fifa2026-<iniciais>-<rand>` (Standard tier, East US 2) → queue `tickets-purchase` (lock 30s, max delivery 10, DLQ habilitada).

3. **AC-3 — PurchaseEntryFunction implementada:** Function HTTP trigger `POST /api/v2/purchase` em .NET 8 isolated worker, recebe body `{ matchId, category: VIP|Cat1|Cat2, userId, quantity }`, gera correlationId (GUID), publica mensagem em `tickets-purchase` via output binding declarativo, retorna `{ correlationId, status: "queued" }` em < 100ms.

4. **AC-4 — PurchaseConsumerFunction implementada:** Function Service Bus trigger consome `tickets-purchase`, valida idempotência (`SELECT 1 FROM purchases WHERE correlation_id = @id`), insere registro em SQL Server com `source='v2'` e `correlation_id` preenchido, atualiza status para `completed`.

5. **AC-5 — Schema delta aplicado em pré-workshop:** migration `phase-01.sql` idempotente adiciona colunas `source NVARCHAR(20) NOT NULL DEFAULT 'v1'` + `correlation_id UNIQUEIDENTIFIER NULL` + **UNIQUE constraint filtered `UQ_purchases_correlation_id (correlation_id) WHERE correlation_id IS NOT NULL`** + index. Migração rodada ANTES da aula, não durante. **Conforme ADE-000 Invariante 4 (idempotência robusta via constraint).**

6. **AC-6 — Idempotência verificável via UNIQUE constraint + INSERT-catch (NÃO SELECT-then-INSERT):** enviar a mesma mensagem 2x resulta em **1** registro em SQL. Consumer captura `SqlException ex when ex.Number == 2627` (unique violation) e ignora duplicata silenciosamente. Teste documentado. **Pattern obrigatório por ADE-000 Invariante 4 (evita TOCTOU race condition).**

7. **AC-7 — Dead-letter queue funcional:** mensagem com `matchId` inválido (e.g., 99999) vai para DLQ após 10 tentativas. Aluno demonstra reprocessamento manual via Service Bus Explorer.

8. **AC-8 — Endpoint de status:** `GET /api/v2/purchase/{correlationId}` retorna `{ status: queued|processing|completed|failed, ticketId?: int }` em < 200ms.

9. **AC-9 — Observabilidade:** Application Insights configurado, mostra trace com correlationId atravessando entry + consumer (distributed tracing W3C). Aluno consegue buscar pelo correlationId no App Insights.

10. **AC-10 — CI/CD funcional:** push para `phase-01-servicebus-functions` dispara workflow GitHub Actions que faz `dotnet publish` → deploy via `Azure/functions-action@v1` → smoke test final com `curl` validando `.correlationId` na resposta.

11. **AC-11 — 6 artefatos didáticos entregues** em `docs/workshops/phase-01/`:
    - `README.md` (leitura prévia, ~300-500 linhas)
    - `PORTAL-GUIDE.md` (passo-a-passo Portal com placeholders de print, ~150-300 linhas)
    - `SPEAKER-NOTES.md` (notas do facilitador por bloco, ~200-400 linhas)
    - `slides.md` (Reveal.js OU markdown convertível, ~30-50 slides)
    - `intro-video-script.md` (~5min de vídeo, roteiro)
    - Branch + workflow CI/CD (a própria branch criada)

12. **AC-12 — Roteiro de 6h respeitado:** 7 blocos conforme blueprint (Conceitos 50min · Portal SB 45min · Entry Function 60min · ☕15min · Consumer Function 60min · DLQ lab 45min · CI/CD 40min · Retro 45min). Documentado no SPEAKER-NOTES.

13. **AC-13 — Anti-hallucination (Art. IV — No Invention):** cada passo técnico rastreável a source: blueprint (`docs/workshops/2026-blueprint-living-lab-azure.md` seção 4.F1), `fifa2026-api/database/schema.sql` (nome real da tabela `purchases`), `fifa2026-api/.env.example`. NÃO inventar comandos, paths, ou nomes de tabela.

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** API + Integration (Functions HTTP/Service Bus triggers, mensageria distribuída)
**Secondary Type(s):** Database (schema delta), Deployment (CI/CD), Documentation (4 artefatos didáticos)
**Complexity:** HIGH (primeira story que monta o esqueleto da arquitetura v2; padrão herdado por F2-F6)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (Dex) — implementação .NET, Functions, CI/CD workflow
- @analyst (Atlas) — produção dos artefatos didáticos (README, PORTAL-GUIDE, SPEAKER-NOTES, slides)

**Supporting Agents:**
- @architect (Aria) — quality gate, validação de padrões herdados por F2-F6
- @devops (Gage) — push final, smoke test em prod, configuração de secrets

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run before marking story complete (CodeRabbit light, max 2 iter, CRITICAL/HIGH only)
- [ ] Pre-PR (@devops): Run before creating pull request (CodeRabbit check, report_only)
- [ ] Pre-Deployment (@devops): Smoke test pós-deploy validando `.correlationId`

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (block if persists after 2 iter)
- HIGH issues: auto_fix (document if fails)

### CodeRabbit Focus Areas

**Primary Focus:**
- Idempotência no consumer (SELECT antes do INSERT — risco de race condition)
- Error handling: try-catch em ambas Functions, propagação correta para DLQ

**Secondary Focus:**
- Connection string sem `EntityPath=...` no App Setting (padrão Functions binding)
- `MaxConcurrentCalls` ajustado no `host.json` para não exaurir SQL pool

---

## Tasks / Subtasks

- [ ] **Task 1 — Setup do branch e estrutura .NET** (AC: 1, 10)
  - [ ] 1.1 Criar branch `phase-01-servicebus-functions` a partir de `main`
  - [ ] 1.2 Criar diretório `src/Fifa2026.V2.Functions/` com `Fifa2026.V2.Functions.csproj` (.NET 8 isolated worker)
  - [ ] 1.3 Adicionar `host.json` com `maxConcurrentCalls=4`, `prefetchCount=0`, `autoCompleteMessages=true`
  - [ ] 1.4 Configurar `local.settings.json` template (sem secrets) e `.gitignore` para excluir versão local

- [ ] **Task 2 — Schema delta** (AC: 5, 13)
  - [ ] 2.1 Criar `fifa2026-api/database/migrations/phase-01.sql` com ALTER TABLE idempotente + index
  - [ ] 2.2 Documentar em README como rodar migration ANTES do workshop (não durante)

- [ ] **Task 3 — PurchaseEntryFunction** (AC: 3, 9)
  - [ ] 3.1 Implementar HTTP trigger POST `/api/v2/purchase` em .NET 8 isolated
  - [ ] 3.2 Validar body com DataAnnotations ou FluentValidation
  - [ ] 3.3 Gerar correlationId (Guid.NewGuid())
  - [ ] 3.4 Output binding Service Bus declarativo (`[ServiceBusOutput("tickets-purchase")]`)
  - [ ] 3.5 Retornar `{ correlationId, status: "queued" }` com 202 Accepted
  - [ ] 3.6 Logar trace com correlationId em ILogger

- [ ] **Task 4 — PurchaseConsumerFunction** (AC: 4, 6, 7, 9)
  - [ ] 4.1 Implementar Service Bus trigger consumindo `tickets-purchase`
  - [ ] 4.2 **Idempotência via UNIQUE constraint + INSERT-catch (conforme ADE-000 Inv 4):** usar `Dapper` ou `Microsoft.Data.SqlClient` com **parameterized queries** para INSERT em `purchases`. Capturar `SqlException ex when ex.Number == 2627` (unique key violation) e tratar como duplicata silenciosa (idempotência atingida). NÃO usar SELECT-then-INSERT (TOCTOU race).
  - [ ] 4.3 INSERT contempla `source='v2'`, `correlation_id`, `status='completed'`, `user_id`, `ticket_category_id`, `quantity`, `unit_price` + `total_price` (JOIN com `ticket_categories` para pegar preços), `created_at` (DEFAULT)
  - [ ] 4.4 Tratar exceções não-2627 → mensagem retorna para queue → após 10 tentativas vai para DLQ automaticamente
  - [ ] 4.5 Logar trace com correlationId em `ILogger.BeginScope(new { CorrelationId })` para propagação automática via .NET Activity API (W3C Trace Context)

- [ ] **Task 5 — Endpoint de status** (AC: 8)
  - [ ] 5.1 Implementar HTTP trigger GET `/api/v2/purchase/{correlationId}`
  - [ ] 5.2 SELECT `purchases` por `correlation_id`, retornar status mapeado

- [ ] **Task 6 — CI/CD Workflow** (AC: 10)
  - [ ] 6.1 Criar `.github/workflows/deploy-phase-01.yml` (template no blueprint AC-10)
  - [ ] 6.2 Configurar secrets `PHASE01_FUNCTION_PUBLISH_PROFILE` e vars `PHASE01_FUNCTION_APP_NAME` (via @devops)
  - [ ] 6.3 Smoke test final com curl + jq validando `.correlationId`

- [ ] **Task 7 — Artefatos didáticos (delegar para @analyst em paralelo)** (AC: 11)
  - [ ] 7.1 `docs/workshops/phase-01/README.md` — leitura prévia
  - [ ] 7.2 `docs/workshops/phase-01/PORTAL-GUIDE.md` — Steps 1-4 (RG → SB Namespace → Queue → Connection String)
  - [ ] 7.3 `docs/workshops/phase-01/SPEAKER-NOTES.md` — notas por bloco (Pontos a enfatizar / Perguntas pra turma / Armadilhas / Se sobrar tempo / Se faltar tempo / Transição)
  - [ ] 7.4 `docs/workshops/phase-01/slides.md` — apresentação Reveal.js
  - [ ] 7.5 `docs/workshops/phase-01/intro-video-script.md` — roteiro de ~5min

- [ ] **Task 8 — Validação ponta-a-ponta** (AC: 6, 7, 9)
  - [ ] 8.1 Smoke test fluxo v2: POST → ver mensagem na queue → ver consumer gravar → GET status = completed
  - [ ] 8.2 Smoke test idempotência: enviar 2x mesma mensagem → 1 registro em SQL
  - [ ] 8.3 Smoke test DLQ: enviar mensagem com matchId=99999 → mensagem na DLQ após 10 tentativas
  - [ ] 8.4 Validar trace no App Insights com correlationId

- [ ] **Task 9 — Anti-hallucination check** (AC: 13)
  - [ ] 9.1 Validar nome real da tabela em `fifa2026-api/database/schema.sql` (assumido `purchases`)
  - [ ] 9.2 Validar conexão string format atualmente usado por `fifa2026-api`
  - [ ] 9.3 NÃO inventar nomes de colunas além de `source` e `correlation_id`

---

## Dev Notes

### Arquitetura técnica delta (do blueprint)

```
[Browser]
   │ POST /api/v2/purchase
   ▼
[Function App: fifa2026-v2-functions (.NET 8 isolated, Consumption)]
   ├─ PurchaseEntryFunction (HTTP trigger, authLevel: Anonymous em F1)
   │     └─ envia msg → [Service Bus Standard]
   │                       └─ Queue: tickets-purchase
   │                             └─ DLQ auto: tickets-purchase/$DeadLetterQueue
   └─ PurchaseConsumerFunction (Service Bus trigger)
         └─ INSERT em SQL Server (mesma DB do v1)
```

> **NÃO toca:** Node API (`fifa2026-api/`), frontend Vite (apenas adiciona botão "Comprar v2" no `Lovable/.../`), SQL schema só ganha 2 colunas idempotentes.

### Endpoints novos

| Verbo | Path | Body | Resposta | Latência alvo |
|---|---|---|---|---|
| POST | `/api/v2/purchase` | `{ matchId, category, userId, quantity }` | `{ correlationId, status: "queued" }` | < 100ms |
| GET | `/api/v2/purchase/{correlationId}` | — | `{ status, ticketId? }` | < 200ms |

### Schema delta (migration `phase-01.sql`, idempotente — conforme ADE-000 Inv 2 + 4)

```sql
IF NOT EXISTS (SELECT 1 FROM sys.columns WHERE Name='source' AND Object_ID=Object_ID('purchases'))
    ALTER TABLE purchases ADD source NVARCHAR(20) NOT NULL DEFAULT 'v1';

IF NOT EXISTS (SELECT 1 FROM sys.columns WHERE Name='correlation_id' AND Object_ID=Object_ID('purchases'))
    ALTER TABLE purchases ADD correlation_id UNIQUEIDENTIFIER NULL;

-- UNIQUE constraint filtered: idempotência robusta (ADE-000 Inv 4)
IF NOT EXISTS (SELECT 1 FROM sys.indexes WHERE Name='UQ_purchases_correlation_id')
    CREATE UNIQUE INDEX UQ_purchases_correlation_id
        ON purchases(correlation_id)
        WHERE correlation_id IS NOT NULL;
```

> **Por que UNIQUE filtered (não constraint regular?):** `correlation_id` é NULL para rows v1 históricas; UNIQUE regular falharia. Filtered index permite UNIQUE apenas onde NOT NULL.

### GitHub Actions workflow (esqueleto)

```yaml
name: Deploy Phase 01 — Service Bus + Functions
on:
  push:
    branches: [phase-01-servicebus-functions]
  workflow_dispatch:
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '8.0.x' }
      - run: dotnet publish src/Fifa2026.V2.Functions -c Release -o ./publish
      - uses: Azure/functions-action@v1
        with:
          app-name: ${{ vars.PHASE01_FUNCTION_APP_NAME }}
          package: ./publish
          publish-profile: ${{ secrets.PHASE01_FUNCTION_PUBLISH_PROFILE }}
      - name: Smoke test
        run: |
          curl -fsS https://${{ vars.PHASE01_FUNCTION_APP_NAME }}.azurewebsites.net/api/v2/purchase \
            -H "Content-Type: application/json" \
            -d '{"matchId":1,"category":"VIP","userId":1,"quantity":1}' | tee response.json
          jq -e '.correlationId' response.json
```

### Roteiro de aula (6h = 360min)

| # | Bloco | Tempo | Modo |
|---|---|---|---|
| 1 | Conceitos: sync vs async, SB vs SQ vs EG, anatomia SB | 50min | Aula expositiva + Q&A |
| 2 | **Provisioning SB via Portal Azure (passo-a-passo)** | 45min | Demo guiada Portal — instrutor projeta, alunos replicam |
| 3 | PurchaseEntryFunction (live coding) | 60min | Live coding |
| ☕ | Coffee break | 15min | — |
| 4 | PurchaseConsumerFunction (live coding) | 60min | Live coding |
| 5 | DLQ + Failures lab | 45min | Lab investigativo |
| 6 | CI/CD via GitHub Actions + smoke test | 40min | Hands-on |
| 7 | Retro + Q&A + carry-over para F2 | 45min | Conversa |

### Troubleshooting esperado (do blueprint)

| Sintoma | Causa | Mitigação |
|---|---|---|
| Função não consome mensagem | Connection string com `EntityPath=...` no App Setting | Remover `EntityPath` |
| Mensagem reentrega infinita | Lock duration < tempo de processamento | Aumentar para 60s ou `RenewLockAsync` |
| `CommandTimeout` no SQL | Connection pool exaurido | `MaxConcurrentCalls=4` no `host.json` |
| Cold start de 10s na 1ª chamada | Consumption plan free | Aceitar (didático); Premium não nesta fase |
| 401 ao chamar Function | `authLevel: Function` sem chave | Usar `authLevel: Anonymous` em F1 (segurança vira F2 com APIM) |
| `purchase` table not found | Nome real é `purchases` (minúsculo) | Confirmar via `schema.sql` |

### Correlation propagation pattern (preparatório para F6 Flow Visualizer — ADE-000 Inv 5)

- **HTTP hops:** header `traceparent` (W3C Trace Context) + `X-Correlation-ID` (compat)
- **Service Bus hops:** `ApplicationProperties["CorrelationId"]` + .NET `Activity.Current.TraceId` propagação automática (Azure.Messaging.ServiceBus ≥ 7.17)
- **Log hops:** `ILogger.BeginScope(new { CorrelationId = id })` — App Insights captura via `customDimensions.CorrelationId`
- **SQL hops:** coluna `correlation_id UNIQUEIDENTIFIER` (ver schema delta)

Em .NET 8 isolated worker, `System.Diagnostics.Activity.Current` é alimentado automaticamente pelos SDKs (Functions Worker, ServiceBus, HttpClient) — não precisa criar Activity manualmente em F1.

### Source files referenciados

- `docs/workshops/2026-blueprint-living-lab-azure.md` (seção 4.F1)
- `docs/epics/EPIC-002-living-lab-workshop.md`
- `docs/architecture/ade-000-microservice-parallel-pattern.md` (foundational — 7 invariantes)
- `docs/qa/2026-05-25-architect-review-S2.1.md` (review report)
- `fifa2026-api/database/schema.sql` (validar nome `purchases` ✅ confirmado)
- `fifa2026-api/.env.example`
- `.github/workflows/deploy-backend.yml` (pattern de CI/CD existente)

### Testing

**Test file location:**
- `src/Fifa2026.V2.Functions.Tests/` (xUnit + Moq)

**Test standards:**
- Unit tests: cobrir validação de body, idempotência (mock IDbConnection), happy path
- Integration tests: Azure Functions Test Host + Service Bus emulator (Testcontainers)
- Smoke test: curl + jq no workflow CI/CD (AC-10)

**Frameworks:**
- xUnit 2.6+
- Moq 4.20+
- Microsoft.Azure.Functions.Worker.Testing.Host

**Specific requirements:**
- Cobertura mínima: 70% das Functions
- Smoke test no CI/CD obrigatório (não pode falhar)

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-05-25 | 0.1.0 | Story drafted from EPIC-002 + blueprint | @sm (River) |
| 2026-05-25 | 0.2.0 | Validated GO (9/10) — Status: Draft → Ready | @po (Pax) |
| 2026-05-25 | 0.3.0 | Architecture review GO + patch obrigatório aplicado: idempotência via UNIQUE constraint + INSERT-catch (ADE-000 Inv 4); W3C Trace Context propagation; parameterized queries explícito | @architect (Aria) |

---

## Dev Agent Record

### Agent Model Used
*(populated by @dev during implementation)*

### Debug Log References
*(populated by @dev during implementation)*

### Completion Notes List
*(populated by @dev during implementation)*

### File List
*(populated by @dev during implementation)*

---

## QA Results
*(populated by @qa during review)*
